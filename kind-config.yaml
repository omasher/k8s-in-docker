# This configuration creates one main node that has the kubernetes control plane
# components and two worker nodes.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker